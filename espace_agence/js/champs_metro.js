// JavaScript Document

// intervenant 1
  function unhide(divID) {
    var item = document.getElementById('fields_2');
    if (item) {
      item.className=(item.className=='hidden')?'unhidden':'hidden';
    }
  }
  
  function hide(divID){
	var item = document.getElementById('hide');
	if(item){
			item.style.visibility='hidden';
			item.className=(item.className=='unhidden')?'hide':'unhidden';
		} 
  }

// intervenant 2
  function unhide_2(divID) {
    var item = document.getElementById('fields_3');
    if (item) {
      item.className=(item.className=='hidden')?'unhidden':'hidden';
    }
  }
  
  function hide_2(divID){
	var item = document.getElementById('hide_2');
	if(item){
			item.style.visibility='hidden';
			item.className=(item.className=='unhidden')?'hide':'unhidden';
		} 
  }
  
  // intervenant 3
  function unhide_3(divID) {
    var item = document.getElementById('fields_4');
    if (item) {
      item.className=(item.className=='hidden')?'unhidden':'hidden';
    }
  }
  
  function hide_3(divID){
	var item = document.getElementById('hide_3');
	if(item){
			item.style.visibility='hidden';
			item.className=(item.className=='unhidden')?'hide':'unhidden';
		} 
  }
  
  // intervenant 4
  function unhide_4(divID) {
    var item = document.getElementById('fields_5');
    if (item) {
      item.className=(item.className=='hidden')?'unhidden':'hidden';
    }
  }
  
  function hide_4(divID){
	var item = document.getElementById('hide_4');
	if(item){
			item.style.visibility='hidden';
			item.className=(item.className=='unhidden')?'hide':'unhidden';
		} 
  }
  
  // intervenant 5
  function unhide_5(divID) {
    var item = document.getElementById('fields_5');
    if (item) {
      item.className=(item.className=='hidden')?'unhidden':'hidden';
    }
  }